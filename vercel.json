{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [{ "src": "/", "dest": "/dist/index.html" }],
  "rewrites": [{ "source": "/api/(.*)", "destination": "/routes" }]
}
